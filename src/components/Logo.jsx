import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <div className="logo">
      <Link>
        {/* <img src={logo} alt="logo" /> */}
        Logo
      </Link>
    </div>
  );
};

export default Logo;
